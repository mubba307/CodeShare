export default function Head() {
  // Disable custom head to rely on Next.js metadata API in layout.
  // This avoids legacy head rendering paths that can trigger internal helper fetches.
  return null;
}
